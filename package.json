{
  "name": "typescript-schema-tools",
  "description": "Schema tooling",
  "version": "2.14.1",
  "license": "MIT",
  "keywords": [
    "library"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/temando/typescript-schema-tools"
  },
  "scripts": {
    "test": "jest src",
    "test:coverage": "jest src --coverage",
    "lint": "tslint --project tsconfig.json --type-check",
    "release": "cd build && yarn publish",
    "build": "bash -v -e scripts/build",
    "build:link": "yarn build && cd build && yarn link",
    "preversion": "yarn lint && yarn build && yarn test && changelog-verify CHANGELOG.md && git add build/*.d.ts",
    "version": "version-changelog CHANGELOG.md && git add CHANGELOG.md",
    "postversion": "yarn build"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.19",
    "@types/clone": "^0.1.30",
    "@types/fs-extra": "^5.0.0",
    "@types/jest": "^21.1.9",
    "@types/node": "^8.5.2",
    "changelog-verify": "^1.1.0",
    "jest": "^22.0.4",
    "ts-jest": "^22.0.0",
    "ts-node": "^4.1.0",
    "tslint": "^5.8.0",
    "tslint-config-temando": "^1.5.11",
    "typescript": "^2.6.2",
    "version-changelog": "^2.1.0"
  },
  "dependencies": {
    "@jdw/jst": "^2.0.0-beta.15",
    "bluebird": "^3.5.0",
    "clone": "^2.1.1",
    "fs-extra": "^5.0.0",
    "lutils": "^2.4.0",
    "typescript-json-schema": "^0.22.0"
  }
}
